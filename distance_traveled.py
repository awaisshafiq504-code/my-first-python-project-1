# Distance Traveled Calculator
# This program claculates the distance is given velocity and time
# def calculate_distance(velocity, time):
#     distance = velocity * time
#     return distance
v = float(input("Velocity (m/s): "))
t = float(input("Time (s): "))
d = v * t  # Using the formula d = v * t
print(f"The distance traveled is {d} meters.")